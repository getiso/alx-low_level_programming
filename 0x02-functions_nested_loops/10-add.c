#include "main.h"

/**
 * add - adds two integers numbers and returns the result
 *@i: number to be added
 * @j: number to be added
 *
 * Return: the result
 */
int add(int a, int b)
{
	return (a + b);
}
