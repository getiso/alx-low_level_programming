#include "main.h"

/**
 * add - adds two integers numbers and returns the result
 *@x: number to be added
 * @y: number to be added
 *
 * Return: the result
 */
int add(int x, int y)
{
	return (x + y);
}
