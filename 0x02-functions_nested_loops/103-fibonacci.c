#include <stdio.h>

/**
 * main - print even sum from Fibonacci sequences
 * values not exceed 4,000,000
 * Return: Always 0 (Success)
 */
int main(void)
{
	long int x;
	long int y;

	x = 1;
	y = 2;

	for (z = 0; z <= 4000000; z++)
	{
		next_term = x + y;

