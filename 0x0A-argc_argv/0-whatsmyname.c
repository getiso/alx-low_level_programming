#include <stdio.h>
/**
 * main - prints its name followed newline
 * @argc: number of arguments passed
 * @argc: arguments vector of pointer to string
 * Return: Always 0 (Success)
 */
int main(int argc _attribute_((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
